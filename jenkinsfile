pipeline {
    agent any

    stages {
        stage('Build') {
            agent {
                docker {
                    image 'node:18-alpine'
                    reuseNode true
                }
            }
            steps {
                sh '''
                    ls -la
                    npm ci
                    npm run build
                    npm test
                    test -f build/index.html
                    npm --version
                    node --version
                    ls -la
                '''
            }
        }
    }
}
