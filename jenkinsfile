pipeline {
    agent any
    
    stages {
        stage('Run NGINX Container') {
            steps {
                sh 'docker run -d -p 8080:80 --name nginx-container nginx:latest'  // Use official NGINX image
            }
        }
    }
}